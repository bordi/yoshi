name: Build and Deploy Website
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Install 🔧
        run: yarn --cwd './website'

      - name: Deploy 🚀
        run: GIT_USER=github_action GITHUB_HOST=${{ secrets.GITHUB_TOKEN }} yarn --cwd './website' run publish-gh-pages
